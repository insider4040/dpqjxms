#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <conio.h>
#include <math.h>

typedef struct Song {
	char title[100];
	char artist[100];
	char genre;
}song_t;

void find(const song_t* ptr, char keyword);

int main()
{
	char keyword[10] = " ";

	song_t song[8] = {
		{"밤편지", "아이유", "ballad"},
		{"I'm The One", "DJ Khaled", "hip-hop"},
		{"Jealous", "DJ Khaled", "hip-hop"},
		{"한여름밤의 꿀", "San E", "hip-hop"},
		{"서울 밤", "어반자카파", "soul"},
		{"썸머", "GRAY", "soul"},
		{"누구 없소(No One)", "이하이", "dance"},
		{"Sixteen", "Ellie Goulding", "pop"},
	};

	while (1) {
		printf("키워드(제목/아티스트)? ");
		fgets(keyword, 10, stdin);

		if (strcmp(keyword, ".") == 0) {
			return 1;
		}

		char* title = NULL;
		char* artist = NULL;

		for (int i = 0; i < 8; ++i) {
			title = strstr(song[i].title, keyword);
			artist = strstr(song[i].artist, keyword);

			if (title != NULL || artist != NULL) {
				printf("%-20s %-20s %-20s", song[i].title, song[i].artist, song[i].genre);
			}
		}
	}
}

//strstr사용?? 

